% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_validate.R
\name{nn_score_bias}
\alias{nn_score_bias}
\title{Score Bias of a Neural Likelihood}
\usage{
nn_score_bias(
  x,
  n_draws = 100,
  h = 0.01,
  pars = NULL,
  n_check = 40,
  theta = NULL,
  rt_min = 0.001,
  rt_max = 8,
  n_grid = 120,
  support = NULL,
  seed = 1,
  cores = 1,
  ...
)
}
\arguments{
\item{x}{An \code{\link[=nn_cell]{nn_cell()}} (with a control), or a neural-likelihood model
(then \code{...} go to \code{\link[=nn_cell]{nn_cell()}}).}

\item{n_draws}{Number of prior draws (when \code{theta} is \code{NULL}).}

\item{h}{Finite-difference step on the sampled scale.}

\item{pars}{Sampled parameters to differentiate; default all except those
entering the support start.}

\item{n_check}{Number of draws on which the control's score bias is
computed.}

\item{theta}{A matrix of sampled-scale parameter vectors; default draws
from the cell prior.}

\item{rt_min, rt_max, n_grid}{The rt grid past the support start.}

\item{support}{\verb{function(pars)}: the earliest possible time per row of a
natural-scale parameter matrix; default \code{t0} (or the \code{pre} parameter),
else 0.}

\item{seed}{Seed for the prior draws (\code{NULL}: leave the RNG alone).}

\item{cores}{Parallel workers over draws (forked; 1 = serial).}

\item{...}{Arguments for \code{\link[=nn_cell]{nn_cell()}} when \code{x} is a model.}
}
\value{
An \code{nn_score_bias} object: \code{summary} (per parameter: mean \code{g} and
its standard error over draws, the same for the control), the per-draw
values \code{g} and \code{g_control}, and \code{grid_mass}, the probability (per cell) the
rt grid holds at each draw: the estimates are conditional on the grid, so
for slow parameter vectors (much mass beyond \code{rt_max}) the control is not
zero until \code{rt_max} is raised.
}
\description{
The expected score of the network's log-likelihood under the true model,
at the true parameters: for each parameter vector theta* drawn from the
cell prior and each sampled parameter j,
\verb{g_j = d/d theta_j E_\{y ~ p(. | theta*)\}[log f(y | theta)]} at
\verb{theta = theta*}, per trial. An exact likelihood has \code{g = 0} (the expected
score is zero); a network whose error varies with theta has not, and
pulls the posterior in the direction of \code{g}. The same estimator applied to
the analytic control must return about zero: it is part of the estimator,
not an optional check (a control that is not zero means the estimator,
not the network, is off).
}
\details{
The expectation is a quadrature over rt: per design cell and
response, \code{n_grid} log-spaced times from \code{rt_min} to \code{rt_max} past the
support start, weighted by the control's likelihood at theta* times the
interval widths (without the widths the control returns -0.25, not 0),
and normalised by the total weight. The derivative is a central
difference with step \code{h} on the sampled scale (halving \code{h} should
shrink the control by four; at \code{h = 0.05} the control can be a sizeable
fraction of the signal). All likelihoods are those a fit uses: for a
race, the winner's density times the losers' survivors; floored at
\code{min_ll}.

The support start (\code{t0}, or the \code{pre} parameter) moves the support, where
this estimator does not apply; parameters entering it are excluded by
default. Score bias is local: it ranks networks of one architecture but
does not predict SBC bias across architectures; use
\code{\link[=nn_posterior_shift]{nn_posterior_shift()}} for that.
}
\examples{
\donttest{
nn_score_bias(RDMnn, n_draws = 5, n_check = 2)
}
}
