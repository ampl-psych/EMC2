% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_validate.R
\name{nn_posterior_shift}
\alias{nn_posterior_shift}
\title{Posterior Shift of a Neural Likelihood Against Its Analytic Control}
\usage{
nn_posterior_shift(
  x,
  n_datasets = 48,
  n_trials = 400,
  n_draws = 2000,
  seed = 7000,
  cores = 1,
  multistart = 0,
  inflate = 1.5,
  ...
)
}
\arguments{
\item{x}{An \code{\link[=nn_cell]{nn_cell()}} (with a control that can simulate), or a
neural-likelihood model (then \code{...} go to \code{\link[=nn_cell]{nn_cell()}}).}

\item{n_datasets}{Number of simulated data sets.}

\item{n_trials}{Trials per data set (per design cell).}

\item{n_draws}{Importance-sampling draws per data set.}

\item{seed}{Data set \code{i} uses \code{set.seed(seed + i)}.}

\item{cores}{Parallel workers over data sets (forked; 1 = serial).}

\item{multistart}{Extra random BFGS starts per posterior (0: start at the
truth only); with starts, the local network mode is kept as a third
mixture component.}

\item{inflate}{Scale factor of the proposal components' sds.}

\item{...}{Arguments for \code{\link[=nn_cell]{nn_cell()}} when \code{x} is a model.}
}
\value{
An \code{nn_posterior_shift} object: \code{summary} (per parameter:
\code{control} and \code{nn} standardised bias, \code{shift}, \code{sd_ratio}), \code{ess}
(per data set, both posteriors), \code{results} (per data set: truth,
posterior means and sds, modes) and the settings.
}
\description{
What an SBC cell computes, without the sampler: for \code{n_datasets} data
sets simulated from the cell prior (by the control's simulator), the
posterior under the network and under the analytic control, each from a
Laplace approximation refined by importance sampling from a defensive
mixture of both. Reports, per sampled parameter, the standardised bias of
each posterior mean (\verb{mean(post mean - truth) / sd(post mean - truth)}
over data sets, SBC's statistic with the mean for the median), the shift
of the network's posterior mean from the control's in control posterior
sds, and the ratio of posterior sds.
}
\details{
The control column is the check's own control: it should be
within Monte Carlo error of zero (its standard error is about
\code{1 / sqrt(n)}; the print flags values beyond \code{3 / sqrt(n)}). Both
columns share the same truths and data, so their Monte Carlo errors are
strongly correlated: the paired \code{shift} (and the difference between the
columns) measures the network's contribution far more precisely than
either column does. In the NLE project this predicted the standardised bias of
500-replicate SBC cells within about 0.1 (at ~1 minute per model against
hours per cell). Use it to screen, not to certify: importance sampling
from Laplace proposals can miss mass a sampler finds; confirm the
chosen network with an SBC cell and its matched control
(\code{\link[=nn_sbc_cell]{nn_sbc_cell()}}).

Per data set (seed \code{seed + i}): truth drawn from the cell prior
(truncated at +- k sd); \code{n_trials} trials by \code{\link[=make_data]{make_data()}} on the
control's design; a
BFGS mode from the truth (plus \code{multistart} random starts) with the
Hessian's eigenvalues floored at the smallest prior precision; proposal
an equal mixture of the Laplace normals with covariance \code{inflate^2}
times theirs; \code{n_draws} draws with self-normalised weights for each
posterior. Likelihoods as in a fit (floored at \code{min_ll}).
}
\examples{
\donttest{
nn_posterior_shift(RDMnn, n_datasets = 2, n_trials = 100, n_draws = 200)
}
}
