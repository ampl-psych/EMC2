% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_validate.R
\name{nn_sbc_cell}
\alias{nn_sbc_cell}
\title{Run (or Prepare) an SBC Cell for a Neural Likelihood, with Its Control}
\usage{
nn_sbc_cell(
  x,
  trials = 400,
  replicates = 500,
  run_control = TRUE,
  archive_dir = NULL,
  run = TRUE,
  info = list(),
  ...
)
}
\arguments{
\item{x}{An \code{\link[=nn_cell]{nn_cell()}}, or a neural-likelihood model (a cell with the
defaults is built).}

\item{trials, replicates}{Trials per replicate and number of replicates.}

\item{run_control}{Also run the control cell.}

\item{archive_dir}{Directory to write the run into (created); \code{NULL} for
none. Result files go to \verb{results/}, the SBC temporary files next to
them: repeating an interrupted call resumes it (a model whose run
finished is loaded, not repeated).}

\item{run}{\code{FALSE} to only write the bundle (needs \code{archive_dir}).}

\item{info}{Optional named list for the README: \code{branch}, \code{commit},
\code{why}, \code{where}, \code{topic}.}

\item{...}{Passed to \code{\link[=run_sbc]{run_sbc()}} (and on to \code{\link[=fit]{fit()}}), e.g.
\code{cores_per_chain}, \code{cores_for_chains}, \code{stop_criteria}.}
}
\value{
(Invisibly) a list: \code{nn} and \code{control} (the SBC objects),
\code{summary} (a data frame, both models), \code{cell}.
}
\description{
Runs \code{\link[=run_sbc]{run_sbc()}} on a cell's design and prior for the network and, with
\code{run_control = TRUE}, on the same design and prior for the analytic
control: a cell with a matched control that differs from it in exactly one
respect, the likelihood. Summarises both with \code{\link[=nn_sbc_summary]{nn_sbc_summary()}} and, given
\code{archive_dir}, writes the run in the layout of the EMC2 SBC archive
(\code{README.md}, \verb{bundle/}, \verb{results/}; see the archive's \code{ARCHIVING.md}).
}
\details{
With \code{run = FALSE} nothing is fitted: the bundle (the cell as
\code{cell.rds}, a \code{run_cell.R} that re-runs this call from the bundle
directory, and \code{session.txt} with the EMC2 build, BLAS and artefact
lines) is written to \code{archive_dir}, ready to be copied to a cluster. A
card registered by path must exist at the same path there; shipped
artefacts are found by name.

The README is a draft: fields the function cannot know (branch and
commit of the EMC2 checkout, why the cell was run, where) come from
\code{info} or are marked \code{TODO}, and the verdict it writes is provisional
(gate: |standardised bias| <= 0.5 for every parameter; ECDF envelopes and
KS p-values reported). File it following \code{ARCHIVING.md} (add the
\code{INDEX.md} row, commit).

\strong{Cores.} In \code{\link[=run_sbc]{run_sbc()}}, \code{cores_per_chain} is the number of replicates
fitted at once, and each replicate's \code{\link[=fit]{fit()}} also runs its chains in
parallel (\code{cores_for_chains}, by default the number of chains): the
total is their product. For a cell, parallel replicates are the
efficient choice, e.g. \verb{cores_per_chain = 8, cores_for_chains = 1} for 8
cores in all.
}
