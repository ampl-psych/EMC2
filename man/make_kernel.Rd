% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{make_kernel}
\alias{make_kernel}
\title{Create a kernel specification}
\usage{
make_kernel(
  cov_names,
  type,
  par_input = NULL,
  kernel_args = NULL,
  custom_kernel = NULL,
  at = "lR",
  at_mode = "filter",
  reference = NULL
)
}
\arguments{
\item{cov_names}{Character vector of covariate column names to apply the
kernel to.}

\item{type}{Character string specifying the kernel type (e.g. \code{"delta"},
\code{"exp_incr"}). See \code{\link[=trend_help]{trend_help()}} for all options.}

\item{par_input}{Optional character vector of parameter names to use as
additional inputs to the kernel.}

\item{kernel_args}{Optional named list of kernel-specific arguments.
Currently supported:
\describe{
\item{\code{q_reset_column}}{For delta-family kernels (\code{"delta"},
\code{"delta2kernel"}, \code{"delta2lr"}) only. Name of a logical or integer
column in \code{data} indicating trials on which the Q-value should be
reset to \code{q0} before the prediction error is computed.
\code{TRUE}/\code{1} triggers a reset; \code{FALSE}/\code{0} does not.}
\item{\code{belief_reset_column}}{For DBM-family kernels (\code{"beta_binomial"},
\code{"beta_binomial_decay"}, \code{"beta_binomial_window"}, \code{"DBM"}, \code{"TPM"})
only. Name of a logical or integer column in \code{data} indicating trials
on which the predictive distribution should be re-initialised, that is,
all preceding observations should be forgotten.
\code{TRUE}/\code{1} triggers a reset; \code{FALSE}/\code{0} does not.}#'
}}

\item{custom_kernel}{A custom kernel registered with \code{\link[=register_kernel]{register_kernel()}}.
Required when \code{kernel = "custom"}.}

\item{at}{If \code{NULL}, the kernel is applied to every row in \code{dadm}. If a
factor column name (e.g. \code{"lR"}), the kernel is applied only to entries
corresponding to the first level of that factor and fed forward to the
other levels. Defaults to \code{"lR"}. For DDMs, \code{at} should be set to \code{NULL}.}

\item{at_mode}{Controls how the \code{at} factor is used during kernel computation.
\describe{
\item{\code{"filter"} (default)}{Only rows corresponding to the first level of
\code{at} are passed to the kernel. The resulting Q-value is then broadcast
to all other rows within the same trial. Use this when all accumulators
share the same covariate value on a given trial.}
\item{\code{"push"}}{All rows are passed to the kernel, allowing each
accumulator row to carry a different covariate value. Q-value updates
are computed per row but only take effect at the start of the
\emph{next} trial (i.e. the next first-level row of \code{at}), ensuring
that within-trial updates do not contaminate Q-values on other rows of
the same trial. Use this when covariate values differ across
accumulators within a trial (e.g. per-symbol feedback in RL models
with multiple accumulators).}
}}

\item{reference}{Optional numeric scalar, a value of the covariate. If given,
the kernel output is expressed relative to its value at that point:
\code{k(c) - k(reference)}, evaluated row by row with the row's own kernel
parameters. The target parameter then becomes the parameter's value at
\code{c = reference} instead of at \code{c = 0}, which removes the collinearity
between the target parameter and the base weight that arises whenever the
covariate varies over a narrow range that excludes zero (e.g. stop-signal
delays: choose a \code{reference} near the middle of the delays used). It changes
the meaning, and so the prior, of the target parameter. Because it is a
fixed constant, it needs no knowledge of the other trials, so the sampler
and the trial-by-trial simulator see exactly the same model. Rows with a
non-finite covariate are left as the kernel produces them: the \verb{slin_*} and
\verb{sat_*} kernels give 0 there (no trend on that row); every other kernel
expects a finite covariate, so give such rows the covariate value
\code{reference} and they contribute exactly 0. Only for a single, non-sequential,
built-in kernel. Defaults to \code{NULL} (no shift).

Saturating kernels (\verb{slin_*}, \verb{sat_*}, \verb{exp_*}, \verb{pow_*}) become constant
once the covariate is past the point of saturation, and a constant kernel is
absorbed by the base weight and the target parameter. Their shape parameter
is therefore identified only from below unless the covariate has mass on
both sides of the bend: profile the likelihood before interpreting one, and
consider bounding it with a \code{pnorm} transform whose \code{lower}/\code{upper} span the
observed covariate range (see \code{\link[=design]{design()}}).}
}
\value{
An object of class \code{emc2_kernel}.
}
\description{
Create a kernel specification
}
\seealso{
\code{\link[=make_base]{make_base()}}, \code{\link[=make_trend]{make_trend()}}, \code{\link[=trend_help]{trend_help()}}
}
