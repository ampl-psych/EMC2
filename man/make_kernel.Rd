% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{make_kernel}
\alias{make_kernel}
\title{Create a kernel specification}
\usage{
make_kernel(
  cov_names,
  type,
  par_input = NULL,
  kernel_args = NULL,
  custom_kernel = NULL,
  at = "lR",
  at_mode = "filter"
)
}
\arguments{
\item{cov_names}{Character vector of covariate column names to apply the
kernel to.}

\item{type}{Character string specifying the kernel type (e.g. \code{"delta"},
\code{"exp_incr"}). See \code{\link[=trend_help]{trend_help()}} for all options.}

\item{par_input}{Optional character vector of parameter names to use as
additional inputs to the kernel.}

\item{kernel_args}{Optional named list of kernel-specific arguments.
Currently supported:
\describe{
\item{\code{q_reset_column}}{For delta-family kernels (\code{"delta"},
\code{"delta2kernel"}, \code{"delta2lr"}) only. Name of a logical or integer
column in \code{data} indicating trials on which the Q-value should be
reset to \code{q0} before the prediction error is computed.
\code{TRUE}/\code{1} triggers a reset; \code{FALSE}/\code{0} does not.}
}}

\item{custom_kernel}{A custom kernel registered with \code{\link[=register_kernel]{register_kernel()}}.
Required when \code{kernel = "custom"}.}

\item{at}{If \code{NULL}, the kernel is applied to every row in \code{dadm}. If a
factor column name (e.g. \code{"lR"}), the kernel is applied only to entries
corresponding to the first level of that factor and fed forward to the
other levels. Defaults to \code{"lR"}. For DDMs, \code{at} should be set to \code{NULL}.}

\item{at_mode}{Controls how the \code{at} factor is used during kernel computation.
\describe{
\item{\code{"filter"} (default)}{Only rows corresponding to the first level of
\code{at} are passed to the kernel. The resulting Q-value is then broadcast
to all other rows within the same trial. Use this when all accumulators
share the same covariate value on a given trial.}
\item{\code{"push"}}{All rows are passed to the kernel, allowing each
accumulator row to carry a different covariate value. Q-value updates
are computed per row but only take effect at the start of the
\emph{next} trial (i.e. the next first-level row of \code{at}), ensuring
that within-trial updates do not contaminate Q-values on other rows of
the same trial. Use this when covariate values differ across
accumulators within a trial (e.g. per-symbol feedback in RL models
with multiple accumulators).}
}}
}
\value{
An object of class \code{emc2_kernel}.
}
\description{
Create a kernel specification
}
\seealso{
\code{\link[=make_base]{make_base()}}, \code{\link[=make_trend]{make_trend()}}, \code{\link[=trend_help]{trend_help()}}
}
