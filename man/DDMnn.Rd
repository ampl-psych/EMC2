% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_NN.R
\name{DDMnn}
\alias{DDMnn}
\title{The Diffusion Decision Model — Neural-Network Likelihood}
\usage{
DDMnn()
}
\value{
A model list with all the necessary functions for EMC2 to sample
}
\description{
Variant of \link{DDM} whose joint likelihood of response and rt is computed by
trained neural networks (neural likelihood estimation) instead of the
numerical-integral density: a normalizing flow for the rt distribution
conditional on parameters and response, and a classifier for the choice
probability. The shipped likelihood is a five-member seed ensemble
(identical architecture, independent training runs; equal-weight density
mixture), which removes training-seed variability from the approximation.
Evaluation takes microseconds per trial — orders of magnitude faster
than the numerical DDM density.
}
\details{
Parameters, transforms, and interpretation match \link{DDM}:\tabular{lllll}{
   \strong{Parameter} \tab \strong{Transform} \tab \strong{Natural scale} \tab \strong{Default} \tab \strong{Interpretation} \cr
   \emph{v} \tab - \tab [-6, 6] \tab 1 \tab Mean drift rate \cr
   \emph{a} \tab log \tab [0.05, 5] \tab log(1) \tab Boundary separation \cr
   \emph{t0} \tab log \tab [0.05, 1] \tab log(0.2) \tab Non-decision time \cr
   \emph{s} \tab log \tab [0.1, 2] \tab log(1) \tab Within-trial drift SD \cr
   \emph{Z} \tab probit \tab [0.1, 0.9] \tab qnorm(0.5) \tab Relative start point \cr
   \emph{SZ} \tab probit \tab [0.05, 0.99] \tab qnorm(0.1) \tab Start-point variability \cr
   \emph{sv} \tab log \tab [0.01, 4] \tab log(0.5) \tab Between-trial drift SD \cr
   \emph{st0} \tab log \tab [0.01, 0.49] \tab log(0.05) \tab Non-decision variability \cr
}


The natural-scale ranges above are the flows' \strong{training region}
(a bounding hyper-box): parameter vectors outside it are rejected
(likelihood floored at \code{min_ll}), both through the model's \code{bound} and
inside the compiled evaluator. Unlike \link{DDM}, the simplified-model
exceptions \code{sv = 0}, \code{SZ = 0}, \code{st0 = 0} are \strong{not} available — this is
intrinsically the \emph{full} DDM; for near-zero variabilities fix them to
small constants inside the box (e.g., \code{constants = c(sv = log(0.01))}).

Note that unlike \link{DDM}, \code{SZ} here is the raw proportion (the flow's
training parameter), not remapped by \code{2 * SZ * min(Z, 1 - Z)}; data
generation applies that remapping internally so \code{rfun} matches \link{DDM}'s
parameter meaning exactly.

Data generation (\code{rfun}) uses the exact sampler (\code{WienR::rWDM}), so
simulation-based checks compare flow-based inference against exact data.

Wuth, J. (2026). \emph{Likelihood approximation in evidence accumulation
models: A comparison of kernel density and neural likelihood methods}
(MSc thesis, University of Amsterdam).
}
