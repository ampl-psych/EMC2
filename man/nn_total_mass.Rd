% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_validate.R
\name{nn_total_mass}
\alias{nn_total_mass}
\title{Total Probability Mass of a Neural Likelihood}
\usage{
nn_total_mass(
  x,
  theta = NULL,
  n = 50,
  rt_max = 20,
  n_grid = 1000,
  support = NULL,
  seed = 1,
  cores = 1,
  ...
)
}
\arguments{
\item{x}{An \code{\link[=nn_cell]{nn_cell()}}, or a neural-likelihood model (then \code{...} go to
\code{\link[=nn_cell]{nn_cell()}}).}

\item{theta}{A matrix of sampled-scale parameter vectors (columns named
by the cell's sampled parameters); default \code{n} draws from the cell
prior.}

\item{n}{Number of prior draws when \code{theta} is \code{NULL}.}

\item{rt_max}{Upper end of the rt grid (seconds).}

\item{n_grid}{Points per log-spaced grid.}

\item{support}{\verb{function(pars)} giving, for each row of a natural-scale
parameter matrix, the earliest possible time; default \code{t0} (or the
\code{pre} parameter), else 0.}

\item{seed}{Seed for the prior draws (\code{NULL}: leave the RNG alone).}

\item{cores}{Parallel workers over draws (forked; 1 = serial).}

\item{...}{Arguments for \code{\link[=nn_cell]{nn_cell()}} when \code{x} is a model.}
}
\value{
An \code{nn_total_mass} object: \code{summary} (per cell: mean and sd of
\verb{log Z}, the same for the control and for the difference, and the largest
\code{before_support}), and \code{mass} (one row per draw and cell).
}
\description{
Integrates the network's likelihood over rt and sums over responses,
\verb{Z(theta) = sum_R int p(rt, R | theta) drt}, for parameter vectors drawn
from the cell prior, and reports how much \verb{log Z} varies over them. A
normalizing flow integrates to one by construction; a network that
regresses the log-likelihood directly does not, which is harmless while
\verb{log Z} is constant in theta (it cancels from the posterior) and biases
inference where it is not. The same integral of the analytic control, on
the same grid, separates the network from the grid (mass beyond \code{rt_max},
which is large for slow parameter vectors).
}
\details{
The grid, per design cell, is the union of a log-spaced grid
from 1 ms to \code{rt_max} and one from 0.1 ms past the support start (by
default \code{t0}; see \code{support}) to \code{rt_max}, each with \code{n_grid} points;
the integral is a trapezoid sum. Densities are the ones a fit uses,
floored at EMC2's \code{min_ll} (\code{1e-10}), so masses below about \code{1e-9} read
as zero. \code{before_support} is the mass the network puts before the
support start (where the exact density is zero).
}
\examples{
\donttest{
nn_total_mass(RDMnn, n = 5, n_grid = 300)
}
}
