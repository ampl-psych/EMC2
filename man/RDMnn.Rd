% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_NN.R
\name{RDMnn}
\alias{RDMnn}
\title{The Racing Diffusion Model — Neural-Network Likelihood}
\usage{
RDMnn(artefact = "rdm_small")
}
\arguments{
\item{artefact}{Name of a shipped artefact in \code{inst/extdata/flownn}
(default \code{"rdm_small"}).}
}
\value{
A model list with all the necessary functions for EMC2 to sample
}
\description{
Variant of \link{RDM} whose single-accumulator density and survivor functions
are computed by a trained normalizing flow (neural likelihood estimation)
instead of the analytic Wald race expressions.
}
\details{
Parameters, transforms, and defaults match \link{RDM}; the natural-scale ranges
are the flow's \strong{training region}, read from the artefact:\tabular{lllll}{
   \strong{Parameter} \tab \strong{Transform} \tab \strong{Natural scale} \tab \strong{Default} \tab \strong{Interpretation} \cr
   \emph{v} \tab log \tab [0.001, 5] \tab log(1) \tab Evidence-accumulation rate \cr
   \emph{B} \tab log \tab [0.1, 3] \tab log(1) \tab Threshold gap (b = B + A) \cr
   \emph{A} \tab log \tab [0.0001, 2] \tab log(0) \tab Start-point variability \cr
   \emph{t0} \tab log \tab [0.05, 1] \tab log(0) \tab Non-decision time \cr
   \emph{s} \tab log \tab [0.1, 2] \tab log(1) \tab Within-trial noise \cr
}


Unlike \link{RDM}, \code{A = 0} and \code{v = 0} are not available (the flow trained on
\code{A >= 1e-4}, \code{v >= 1e-3}). As for \link{DDMnn}, an un-sampled \code{A} at its default
\code{0} is refused with an error; fix it inside the box instead, e.g.
\code{constants = c(A = log(1e-3))}. Out-of-box parameter vectors are rejected
(likelihood floored at \code{min_ll}).

\code{RDMnn(artefact)} is \code{register_nn_model(artefact, kind = "flow_race", twin = RDM)()}; see \code{\link[=register_nn_model]{register_nn_model()}} for the contract.

Data generation (\code{rfun}) uses the analytic RDM, so simulation-based
checks compare flow-based inference against exact data. Loading a neural
likelihood is not the same as being able to infer with it; validate before
use (\code{\link[=nn_cell]{nn_cell()}} and the validation kit run the checks against \link{RDM}).

Wuth, J. (2026). \emph{Likelihood approximation in evidence accumulation
models: A comparison of kernel density and neural likelihood methods}
(MSc thesis, University of Amsterdam).
}
