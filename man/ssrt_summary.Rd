% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssrt_summary.R
\name{ssrt_summary}
\alias{ssrt_summary}
\title{Summarize posterior SSRT moments for stop-signal models}
\usage{
ssrt_summary(
  emc,
  level = c("population", "individual", "condition"),
  probs = c(0.025, 0.5, 0.975),
  details = FALSE,
  stage = get_last_stage(emc),
  n_population = 1000,
  seed = NULL,
  data = NULL,
  condition_vars = NULL,
  condition_level = c("population", "individual"),
  thin = 1,
  filter = 0,
  length.out = NULL,
  subject = NULL,
  ...
)
}
\arguments{
\item{emc}{A fitted EMC2 object using a stop-signal model.}

\item{level}{Character. \code{"population"} summarizes the marginal population
distribution implied by the hierarchical mean and covariance draws.
\code{"individual"} summarizes each subject's posterior \code{alpha} draws.
\code{"condition"} maps stop parameters to design cells before computing SSRT
moments. For \code{type = "single"} fits, the default is resolved to
\code{"individual"} because no population distribution is sampled.}

\item{probs}{Numeric vector of quantiles to return.}

\item{details}{Logical. If \code{FALSE}, return a compact summary with only the
SSRT mean and standard deviation rows, requested posterior summary columns,
necessary subject/condition identifiers, and \code{n_population} where relevant.
The full detailed summary is retained in the \code{"ssrt_details"} attribute. If
\code{TRUE}, return the detailed summary directly, including technical metadata
and population variance-decomposition rows.}

\item{stage}{Sampling stage passed to \code{\link[=get_pars]{get_pars()}}.}

\item{n_population}{Number of implied population members sampled per
posterior draw when \code{level = "population"}.}

\item{seed}{Optional random seed used for population sampling.}

\item{data}{Optional data frame used to define condition cells when
\code{level = "condition"}. Defaults to the fitted data.}

\item{condition_vars}{Optional character vector of columns defining
condition cells. By default, variables appearing in stop-parameter formulas
are used.}

\item{condition_level}{Character. Only used when \code{level = "condition"}.
\code{"population"} samples implied individuals from each hierarchical
population draw and summarizes each condition. \code{"individual"} maps each
observed subject's \code{alpha} draws and returns subject-by-condition summaries.}

\item{thin, filter, length.out, subject}{Passed to \code{\link[=get_pars]{get_pars()}}.}

\item{...}{Additional arguments passed to \code{\link[=get_pars]{get_pars()}}.}
}
\value{
By default (\code{details = FALSE}), a compact data frame with one row for
each requested SSRT quantity:
\describe{
\item{\code{ssrt_mean}}{The mean stop finishing time on the seconds scale.}
\item{\code{ssrt_sd}}{The total standard deviation of stop finishing times on
the seconds scale. For population summaries this combines the average
within-person stop-time variance and the between-person variance in SSRT
means.}
}
The default output includes the \code{parameter}, posterior \code{mean}, posterior
\code{sd}, requested quantile columns, necessary subject/condition identifiers,
and \code{n_population} for population summaries.

The full detailed summary is stored in \code{attr(x, "ssrt_details")} when
\code{details = FALSE}, where \code{x} is the returned object. Calling
\code{ssrt_summary(..., details = TRUE)} returns this detailed data frame directly.
The detailed output also includes technical metadata columns (\code{level},
\code{stop_family}, and \code{method}) and, for population summaries, the additional
rows:
\describe{
\item{\code{between_subject_sd}}{Returned for population summaries when
\code{details = TRUE}. The
standard deviation of individual SSRT means implied by the hierarchical
population distribution.}
\item{\code{mean_within_subject_sd}}{Returned for population summaries when
\code{details = TRUE}.
The average individual stop-time standard deviation.}
}
In all outputs, quantile columns are named by their percentages, for example
\verb{2.5\%}, \verb{50\%}, and \verb{97.5\%}.
}
\description{
Computes posterior summaries of the stop-time distribution mean and standard
deviation for EMC2 stop-signal models. For individual-level summaries, SSRT
moments are computed from each subject's posterior \code{alpha} draws. For
population-level summaries, implied individuals are sampled from each
posterior draw of the hierarchical multivariate normal distribution before
transforming to seconds.
}
