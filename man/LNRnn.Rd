% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_NN.R
\name{LNRnn}
\alias{LNRnn}
\title{The Log-Normal Race Model — Neural-Network Likelihood}
\usage{
LNRnn()
}
\value{
A model list with all the necessary functions for EMC2 to sample
}
\description{
Variant of \link{LNR} whose single-accumulator density and survivor functions
are computed by a trained normalizing flow (neural likelihood estimation)
instead of the analytic log-normal. Intended for validating the
flow-likelihood workflow against a model whose analytic likelihood is
available for comparison.
}
\details{
Parameters, transforms, and interpretation match \link{LNR}:\tabular{lllll}{
   \strong{Parameter} \tab \strong{Transform} \tab \strong{Natural scale} \tab \strong{Default} \tab \strong{Interpretation} \cr
   \emph{m} \tab - \tab [-3, 3] \tab 1 \tab Scale parameter \cr
   \emph{s} \tab log \tab [0.1, 2] \tab log(1) \tab Shape parameter \cr
   \emph{t0} \tab log \tab [0.05, 1] \tab log(0.1) \tab Non-decision time \cr
}


The natural-scale ranges above are the flow's \strong{training region}
(a bounding hyper-box): parameter vectors outside it are rejected
(likelihood floored at \code{min_ll}), both through the model's \code{bound} and
inside the compiled evaluator. Note the box is much tighter than the
analytic \link{LNR} bounds — priors and start points must respect it.

Data generation (\code{rfun}) uses the analytic LNR, so simulation-based
checks compare flow-based inference against exact data.

Wuth, J. (2026). \emph{Likelihood approximation in evidence accumulation
models: A comparison of kernel density and neural likelihood methods}
(MSc thesis, University of Amsterdam).
}
