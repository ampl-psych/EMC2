% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_ordered.R
\name{ordered_logit}
\alias{ordered_logit}
\title{Ordered Logit Response Model}
\usage{
ordered_logit()
}
\value{
A model list with all the necessary functions to sample
}
\description{
Ordered responses based on a latent logistic evidence variable. Binary
response models are the 2-category special case.
}
\details{
The \code{scale} parameter is the latent logistic scale, and thresholds follow the
same ordered \code{K - 1} parameterization as \code{ordered_probit()}, with derived
ordered thresholds returned in \code{cut_expanded}.
}
\examples{
dord <- design(
  Rlevels = c("left", "right"),
  factors = list(subjects = 1, S = c("left", "right")),
  formula = list(location ~ 0 + S, scale ~ 1, cut ~ 1),
  matchfun = function(d) d$S == d$lR,
  constants = c(scale = log(1)),
  model = ordered_logit
)
}
