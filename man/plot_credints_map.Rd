% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_credints_map}
\alias{plot_credints_map}
\title{Plot all credint(map=TRUE) types in a multi-panel display}
\usage{
plot_credints_map(
  ci,
  quants = 1:3,
  order = NULL,
  layout = NA,
  plot_args = NULL,
  ...
)
}
\arguments{
\item{ci}{Length-1 list returned by \code{credint(..., map=TRUE)}.}

\item{quants}{Integer vector of length 3 (default \code{1:3}).}

\item{order}{\code{NULL} (show all multi-value types in row-name order) or
a character/integer vector selecting and ordering panels.  Unlisted types
are dropped.}

\item{layout}{As in \code{\link{plot_credints}}.}

\item{plot_args}{Named list of per-panel argument lists keyed by type name
or \code{"intercepts"} for the aggregate panel.  Supply \code{factors}
for each multi-value type here, e.g.
\code{plot_args = list(v = list(factors = c("S", "E")))}.}

\item{...}{Additional arguments forwarded to every sub-call.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Automatically classifies parameter types from \code{credint(..., map=TRUE)}
output, aggregates single-value (no factorial structure) types into one
shared-scale panel via \code{\link{plot_credint}}'s vector-type mode, and
produces a \code{\link{plot_credint_map}} panel for each multi-value type.
Factorial structure (\code{factors}) for each multi-value type must be
supplied via \code{plot_args}.
}
\seealso{
\code{\link{plot_credint_map}}, \code{\link{plot_credints}}
}
