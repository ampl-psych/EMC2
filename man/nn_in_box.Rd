% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_validate.R
\name{nn_in_box}
\alias{nn_in_box}
\title{Check that a Prior Lies Inside a Neural Likelihood's Training Region}
\usage{
nn_in_box(x, prior = NULL, k = 4, refuse = TRUE)
}
\arguments{
\item{x}{An \code{\link[=nn_cell]{nn_cell()}}, an \code{emc.design} of a neural-likelihood model, or
an \code{emc.prior} made for one (its design is then taken from the prior).}

\item{prior}{An \code{emc.prior} (made with \code{\link[=prior]{prior()}}) when \code{x} is a design.
For a hierarchical prior the group-mean prior is checked; subjects
spread further, so passing is necessary, not sufficient.}

\item{k}{Half-width in prior sds.}

\item{refuse}{If \code{TRUE}, an error lists every parameter and cell whose
range leaves the region; if \code{FALSE}, the table is returned regardless.}
}
\value{
(Invisibly) a data frame with one row per network input and
design cell: the natural-scale range of the prior at +- \code{k} sd, the
training region, and whether the former lies inside the latter.
}
\description{
For every network input and design cell, maps the prior's mean +- \code{k} sd
(on the sampled scale, through the design matrices) to the natural scale
and compares it with the network's training region. Parameter vectors
outside the region are rejected by the network, so such a prior is
silently truncated in a fit and breaks the calibration of an SBC: every
replicate whose true parameters leave the region cannot be recovered.
\code{\link[=run_sbc]{run_sbc()}} runs this check (with \code{k = getOption("emc.nn_box_k", 4)}) for
every neural-likelihood design, and \code{\link[=nn_cell]{nn_cell()}} for its priors.
}
\examples{
des <- design(factors = list(subjects = 1), Rlevels = c("a", "b"), model = RDMnn,
              formula = list(v ~ 0 + lR, B ~ 1, t0 ~ 1, A ~ 1), constants = c(s = log(1)))
pri <- prior(des, type = "single",
             pmean = c(v_lRa = log(2), v_lRb = log(1), B = 0, t0 = log(.2), A = log(.3)),
             psd = c(v_lRa = .1, v_lRb = .1, B = .1, t0 = .1, A = .1))
nn_in_box(des, pri)
}
