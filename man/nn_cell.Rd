% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_validate.R
\name{nn_cell}
\alias{nn_cell}
\title{A Validation Cell for a Neural-Network Likelihood}
\usage{
nn_cell(
  model,
  mean = NULL,
  sd = NULL,
  constants = NULL,
  formula = NULL,
  factors = NULL,
  Rlevels = NULL,
  matchfun = NULL,
  control = NULL,
  k = 4
)
}
\arguments{
\item{model}{A neural-likelihood model function (\link{DDMnn}, \link{RDMnn}, or one
returned by \code{\link[=register_nn_model]{register_nn_model()}}).}

\item{mean, sd}{Prior means and sds on the sampled scale, named by sampled
parameter (e.g. \code{v_lRa}) or by model parameter (e.g. \code{v}, then used for
every sampled parameter that is the value of \code{v} in some cell). Never
matched by position. \code{sd} may be a single unnamed number. Parameters
that are effects (differences between cells) need explicit values.}

\item{constants}{Constants on the sampled scale, as in \code{\link[=design]{design()}}.}

\item{formula, factors, Rlevels, matchfun}{As in \code{\link[=design]{design()}}; \code{factors}
excludes \code{subjects} (one subject). Defaults: see Details; \code{Rlevels}
\code{c("lower", "upper")} for two-response networks, \code{c("a", "b")} for races.}

\item{control}{The analytic control: \code{NULL} (the model's registered twin),
an EMC2 model function taking the same parameters, or \code{FALSE} (none; the
checks that need a control then refuse).}

\item{k}{Half-width of the prior, in sds, that must lie inside the
training region.}
}
\value{
An \code{nn_cell}: a list with the model, the registration, the design
and prior for the network (\code{design}, \code{prior}) and for the control
(\code{control_design}, \code{control_prior}), the prior \code{mean} and \code{sd} per
sampled parameter, \code{k}, and the box check (\code{box}).
}
\description{
Builds the design and prior on which the neural-likelihood checks
(\code{\link[=nn_in_box]{nn_in_box()}}, \code{\link[=nn_total_mass]{nn_total_mass()}}, \code{\link[=nn_score_bias]{nn_score_bias()}},
\code{\link[=nn_posterior_shift]{nn_posterior_shift()}}, \code{\link[=nn_sbc_cell]{nn_sbc_cell()}}) run: one design (by default a
single cell, every network input sampled with an intercept-only formula),
independent normal priors on the sampled scale that lie inside the
network's training region to \code{k} standard deviations, and the same design
and prior for an analytic \emph{control} model. Those functions also accept a
model directly and then build the cell with the defaults below.
}
\details{
\strong{Why a control.} A check is only interpretable next to the same check
run on the exact likelihood: a score bias or a posterior shift of the
analytic model must come out at about zero, and whatever it does show
(finite-difference error, Monte Carlo error, the simulator, the design) is
not the network's fault. The control cell uses the control model in an
otherwise identical design (same formula, constants, defaults and prior),
so the two cells differ in the likelihood only. The control must take the
same parameters as the network's model, with the same defaults and
transforms; by default it is the analytic twin the network was registered
with (e.g. \link{DDM} for \link{DDMnn}).

\strong{Defaults.} Sampled are the network's inputs (and a parameter used by
\code{pre}), except parameters fixed at registration and a parameter named \code{s}
(the within-trial noise, a scaling parameter: it stays at the model
default, which identifies the model). For a race network the rate varies
over accumulators (\code{v ~ 0 + lR}); every other formula is intercept only.
A prior mean defaults to the model's default where that lies inside the
training region and to the centre of the region otherwise; a prior sd
defaults to 99\% of the largest value that keeps mean +- \code{k} sd inside the
region.
Both are on the sampled scale.

\strong{Priors inside the box.} Parameter vectors outside the training region
are rejected by the network (likelihood floored), which truncates the
posterior to the region. For fitting that is a choice; for SBC it breaks
calibration, because a replicate whose true parameters lie outside the
region cannot be recovered. \code{nn_cell()} therefore refuses priors that
leave the region within \code{k} sd (\code{\link[=nn_in_box]{nn_in_box()}}); \code{\link[=run_sbc]{run_sbc()}} makes the same
check for neural-likelihood designs.
}
\examples{
cell <- nn_cell(RDMnn, mean = c(v = log(1.5), B = log(1), A = log(.3), t0 = log(.2)),
                sd = .1)
cell
}
\seealso{
\code{\link[=nn_in_box]{nn_in_box()}}, \code{\link[=nn_score_bias]{nn_score_bias()}}, \code{\link[=nn_posterior_shift]{nn_posterior_shift()}},
\code{\link[=nn_total_mass]{nn_total_mass()}}, \code{\link[=nn_sbc_cell]{nn_sbc_cell()}}, \code{\link[=register_nn_model]{register_nn_model()}}
}
