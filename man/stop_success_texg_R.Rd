% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stop_success_gl.R
\name{stop_success_texg_R}
\alias{stop_success_texg_R}
\title{Stop-success integral (truncated ex-Gaussian race), R route, method-selectable}
\usage{
stop_success_texg_R(
  mu,
  sigma,
  tau,
  lb,
  SSD,
  upper = Inf,
  method = c("integrate", "gl", "analytic", "auto"),
  n_nodes = 64L,
  k_sigma = 8,
  k_tau = 16
)
}
\arguments{
\item{mu, sigma, tau, lb}{Numeric vectors c(stop, go1, go2, ...) of ex-Gaussian
parameters and truncation lower bounds (stop first, then go accumulators).}

\item{SSD}{Stop-signal delay.}

\item{upper}{Upper integration limit; Inf -> full integral ("integrate") or
heuristic muS + k_sigma\emph{sigS + k_tau}tauS window ("gl").}

\item{method}{"integrate" (adaptive; original route, numerically untouched),
"gl" (fixed Gauss-Legendre), "analytic" or "auto" (exact n_go == 1 closed
form where valid, GL fallback otherwise; see stop_success_texg_analytic1_R).}

\item{n_nodes}{Number of GL nodes when method = "gl" (and the base node
count for the "auto"/"analytic" GL fallback).}

\item{k_sigma, k_tau}{Finite-window heuristic for the GL routes.}
}
\value{
The integral value (probability of successful stop, pre-trigger-failure).
}
\description{
Stop-success integral (truncated ex-Gaussian race), R route, method-selectable
}
\keyword{internal}
